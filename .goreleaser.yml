# This file is autogenerated by [goreleaser](https://github.com/goreleaser/goreleaser).
# Do not edit manually.
# For more information, see https://goreleaser.com.

version: 2

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'

release:
  github:
    owner: R3dc0D4
    name: usbredirect

brews:
  - name: usbredirect
    repository:
      owner: R3dc0D4
      name: homebrew-tap
    directory: Formula
    homepage: https://github.com/R3dc0D4/usbredirect
    description: Cross-platform COM port network redirector
    test: |
      system "#{bin}/usbredirect version"